///-------------------DANS LA METHODE SE FICHIER A ETE SUPPRIMER------------
/*   format normal
const success = (message, data) => {
    return {
        message: message,
        data: data
    }
}

exports.success

En gros cette function rajoute une clé message avec valeur à l'obj JSON pokemon
*/

//------format conssi
/*
exports.success = (message, data) => {
    return {message, data }
}

exports.getUniqueId = (pokemons) => {
    const pokemonsIds = pokemons.map(pokemon => pokemon.id)
    const maxId = pokemonsIds.reduce((a,b) => Math.max(a, b))
    const uniqueId = maxId +1
    return uniqueId
}

*/


///-------------------DANS LA METHODE SE FICHIER A ETE SUPPRIMER------------

